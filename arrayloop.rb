names=Array["Booker","HOney","Chicken","Mutton"]
puts"\n names arrray size:#{names.length}\n\n names of array:"
count=0
while(count<names.length)do
    puts"\nIndex:#{count}names[#{count}]:#{names[count]}"
    count=count+1
end
